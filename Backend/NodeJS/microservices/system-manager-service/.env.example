# ============================================================
# system-manager-service environment — EXAMPLE. Copy to .env
# ============================================================
NODE_ENV=development
PORT=3003

DATABASE_HOST=postgres-system
DATABASE_PORT=5432
DATABASE_USER=clinic_user
DATABASE_PASSWORD=
DATABASE_NAME=system_db

JWT_SECRET=
JWT_EXPIRES_IN=7d

INTERNAL_SERVICE_TOKEN=

KAFKA_BROKERS=kafka-1:9092

ALLOWED_ORIGINS=http://localhost:3000

# Default system manager seed (POST /v1/system-manager/dev/seed-default)
DEFAULT_ADMIN_USERNAME=
DEFAULT_ADMIN_PASSWORD=
DEFAULT_ADMIN_FIRST_NAME=
DEFAULT_ADMIN_LAST_NAME=
DEFAULT_ADMIN_EMAIL=
DEFAULT_ADMIN_PHONE=
