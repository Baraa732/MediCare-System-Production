# MongoDB (Evolution API)

WhatsApp gateway instance state and message persistence.

| Compose service | Database | Used by |
|-----------------|----------|---------|
| `mongodb` | `evolution` | evolution-api |

Credentials: root `.env` → `MONGO_USER`, `MONGO_PASSWORD`.

See also: `../README.md`, `../evolution-api/README.md`
