# kafka-init — Railway one-shot / job (placeholders only)
# Build: Dockerfile Messaging/Kafka/scripts/Dockerfile.kafka-init, context = repo root
# Run once after kafka-1 is healthy. Safe to re-run (idempotent --if-not-exists).
KAFKA_BROKERS=kafka-1.railway.internal:9092
