# Vite dev server proxies all /api/* calls to this NestJS gateway target.
# The browser always talks to the dashboard origin (http://localhost:3002), never the gateway directly.
VITE_API_PROXY_TARGET=http://localhost:3000
